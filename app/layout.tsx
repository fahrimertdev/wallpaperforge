import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "WallpaperForge — Create stunning wallpapers in seconds",
  description:
    "Generate custom phone and desktop wallpapers using beautiful templates, color palettes, and visual effects. No account needed.",
  openGraph: {
    title: "WallpaperForge",
    description: "Create stunning wallpapers in seconds.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full`}
    >
      <body className="h-full flex flex-col">{children}</body>
    </html>
  );
}
